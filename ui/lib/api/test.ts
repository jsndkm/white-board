// import { fetcher } from "@/lib/api/index";
//
// const test = `${process.env.NEXT_PUBLIC_BASE_URL}/api/excalidraw`;
//
// export const uploadExcalidraw = async (d: never | undefined) => {
//   if (!d) return;
//   await fetcher<null>(test, {
//     method: "POST",
//     body: JSON.stringify(d),
//   });
// };
